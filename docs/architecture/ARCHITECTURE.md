# 🏗️ Architecture du Projet NAHB

## Vue d'ensemble

```
┌─────────────────────────────────────────────────────────────────┐
│                       CLIENT LAYER (Frontend)                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ React 18 + Vite                                            │ │
│  │ - Pages (Auth, Reader, Author, Admin)                      │ │
│  │ - Components (Story, Page, Choice, Game)                   │ │
│  │ - Context API (AuthContext, GameContext)                   │ │
│  │ - Services (API calls via Axios)                           │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↕ HTTP/REST
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND LAYER (API Server)                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Express.js Server                                          │ │
│  │ ┌──────────────────────────────────────────────────────┐   │ │
│  │ │ Routes (45+ endpoints)                               │   │ │
│  │ │ - /api/auth, /api/stories, /api/pages, /api/choices │   │ │
│  │ │ - /api/game, /api/ratings, /api/admin, etc.         │   │ │
│  │ └──────────────────────────────────────────────────────┘   │ │
│  │ ┌──────────────────────────────────────────────────────┐   │ │
│  │ │ Controllers (15 fichiers)                            │   │ │
│  │ │ - authController, storyController, pageController    │   │ │
│  │ │ - gameController, ratingController, adminController  │   │ │
│  │ └──────────────────────────────────────────────────────┘   │ │
│  │ ┌──────────────────────────────────────────────────────┐   │ │
│  │ │ Services (Business Logic)                            │   │ │
│  │ │ - UserService, StoryService, GameService            │   │ │
│  │ │ - StatisticsService, NotificationService            │   │ │
│  │ └──────────────────────────────────────────────────────┘   │ │
│  │ ┌──────────────────────────────────────────────────────┐   │ │
│  │ │ Middleware                                           │   │ │
│  │ │ - auth.js (JWT verification)                         │   │ │
│  │ │ - roleCheck.js (RBAC - Reader/Author/Admin)          │   │ │
│  │ │ - validation.js (Input validation)                   │   │ │
│  │ │ - errorHandler.js (Error management)                 │   │ │
│  │ └──────────────────────────────────────────────────────┘   │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↕ SQL
┌─────────────────────────────────────────────────────────────────┐
│                    DATA LAYER (Database)                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Prisma ORM + MySQL 8                                       │ │
│  │ - 20+ Models (User, Story, Page, Choice, etc.)            │ │
│  │ - Automatic migrations                                    │ │
│  │ - Type-safe queries                                       │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Flux de Données Principal

### Scénario 1 : Lecture d'une histoire

```
┌──────────┐
│ Lecteur  │
└────┬─────┘
     │ Clique "Lire"
     ↓
┌─────────────────────────────┐
│ Frontend - StoryReader Page  │
│ - GET /api/stories/:id      │
│ - GET /api/pages/story/:id  │
│ - GET /api/choices/:pageId  │
└────────┬────────────────────┘
         │
         ↓
┌─────────────────────────────────────────┐
│ Backend Controllers                      │
│ - storyController.getStory()            │
│ - pageController.getPagesByStory()      │
│ - choiceController.getChoicesByPage()   │
└────────┬────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────┐
│ Services (Business Logic)                │
│ - StoryService.formatStory()            │
│ - GameService.calculateStats()          │
└────────┬────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────┐
│ Prisma ORM Queries                       │
│ - prisma.story.findUnique()             │
│ - prisma.page.findMany()                │
│ - prisma.choice.findMany()              │
└────────┬────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────┐
│ MySQL Database                           │
│ - SELECT * FROM stories WHERE id=...    │
│ - SELECT * FROM pages WHERE storyId=... │
│ - SELECT * FROM choices WHERE pageId=...│
└────────┬────────────────────────────────┘
         │
         ↓ JSON Response
┌──────────────────────────────┐
│ Frontend - Display Page       │
│ + Text                        │
│ + Available Choices           │
│ + Stats                       │
└──────────────────────────────┘
```

---

### Scénario 2 : Créer une histoire (Auteur)

```
┌──────────┐
│ Auteur   │
└────┬─────┘
     │ Crée histoire
     ↓
┌──────────────────────────────────────┐
│ Frontend - CreateStory Modal          │
│ - Title, Description, Tags           │
│ - POST /api/stories                  │
└────────┬─────────────────────────────┘
         │ (user_id in JWT)
         ↓
┌──────────────────────────────────────┐
│ Backend                               │
│ - auth middleware (verify JWT)       │
│ - roleCheck (AUTHOR)                 │
│ - storyController.createStory()      │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│ Validation                            │
│ - validateStory(title, desc, tags)   │
│ - Check title length, etc.           │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│ Service Layer                         │
│ - StoryService.createStory()         │
│ - Auto-generate UUID                 │
│ - Set status=DRAFT                   │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│ Prisma Create                         │
│ prisma.story.create({                │
│   title, description, tags,          │
│   authorId, status: DRAFT            │
│ })                                   │
└────────┬─────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────┐
│ MySQL INSERT                          │
│ INSERT INTO stories (...)            │
│ VALUES (...)                         │
└────────┬─────────────────────────────┘
         │
         ↓ Response with storyId
┌──────────────────────────────────────┐
│ Frontend - Redirect to Editor         │
│ Now user can add pages & choices      │
└──────────────────────────────────────┘
```

---

## 🗂️ Couches et Responsabilités

### 1. **Presentation Layer (Frontend)**
- **Technologies** : React, Vite, Axios
- **Responsabilité** : 
  - Affichage du contenu
  - Gestion des événements utilisateur
  - Navigation entre pages
  - Gestion d'état local (Context API)
- **Fichiers** : `components/`, `pages/`, `services/api.js`

### 2. **API Layer (Backend)**
- **Route Handler** : Express routes
- **Responsabilité** :
  - Recevoir les requêtes HTTP
  - Vérifier les tokens JWT
  - Valider les données
  - Déléguer la logique métier
  - Retourner les réponses JSON
- **Fichiers** : `routes/`, `controllers/`, `middleware/`

### 3. **Business Logic Layer (Services)**
- **Responsabilité** :
  - Logique métier complexe
  - Calcul des statistiques
  - Gestion des transitions d'état
  - Orchestration de plusieurs modèles
  - Caching optionnel
- **Fichiers** : `services/`

### 4. **Data Access Layer (ORM)**
- **Outil** : Prisma ORM
- **Responsabilité** :
  - Type-safe queries
  - Relationships handling
  - Migrations management
  - Query optimization
- **Fichiers** : `prisma/schema.prisma`

### 5. **Database Layer**
- **SGBD** : MySQL 8.0
- **Responsabilité** :
  - Stockage persistant
  - Indexation
  - Intégrité des données
  - Contraintes

---

## 🔐 Authentification et Autorisation

### Flow Authentification (JWT)

```
1. Login
   ┌─────────┐
   │ User    │ POST /api/auth/login (email, password)
   └────┬────┘
        │
        ↓
   ┌───────────────────────────────┐
   │ authController.login()         │
   │ - Find user by email          │
   │ - Verify password (bcrypt)    │
   └────┬────────────────────────────┘
        │
        ↓
   ┌───────────────────────────────┐
   │ Generate JWT Token             │
   │ - Payload: {id, email, role}  │
   │ - Secret: JWT_SECRET           │
   │ - Expiry: 7 days               │
   └────┬─────────────────────────────┘
        │
        ↓
   ┌───────────────────────────────┐
   │ Return { token, user }        │
   │ Frontend stores in localStorage│
   └───────────────────────────────┘

2. Requête Authentifiée
   ┌──────────────────┐
   │ GET /api/stories │ Header: Authorization: Bearer <token>
   └────┬─────────────┘
        │
        ↓
   ┌───────────────────────────────┐
   │ auth middleware               │
   │ - Extract token from header   │
   │ - Verify signature (JWT_SECRET)│
   │ - Decode payload              │
   │ - Add user to req.user        │
   └────┬─────────────────────────────┘
        │
        ↓
   ┌──────────────────────────┐
   │ Controller execute       │
   │ req.user = { id, role }  │
   └──────────────────────────┘

3. Autorisation (RBAC)
   ┌──────────────────────────┐
   │ POST /api/stories (Créer)│
   │ Rôle requis: AUTHOR      │
   └────┬─────────────────────┘
        │
        ↓
   ┌──────────────────────────────────┐
   │ roleCheck('author', 'admin')     │
   │ - Check req.user.role            │
   │ - If role in ['AUTHOR', 'ADMIN']│
   │   → Continuer                    │
   │ - Else → 403 Forbidden           │
   └──────────────────────────────────┘
```

---

## 🔄 Patterns et Décisions Architecturales

### 1. **Separation of Concerns**
- Routes → Controllers → Services → Models
- Chaque couche a une responsabilité unique

### 2. **RESTful API**
- GET /stories (list)
- POST /stories (create)
- GET /stories/:id (read)
- PUT /stories/:id (update)
- DELETE /stories/:id (delete)

### 3. **Error Handling**
```javascript
// Centralized error handler middleware
app.use((err, req, res, next) => {
  if (err.statusCode === 401) {
    return res.status(401).json({ error: 'Unauthorized' });
  }
  // ... other error types
  res.status(500).json({ error: 'Internal Server Error' });
});
```

### 4. **Validation Layer**
- Input validation avant le traitement
- Schema validation (Joi/Zod)
- Database constraint validation

### 5. **Database Relationships**
- One-to-Many : User ↔ Stories
- Many-to-One : Choices → Pages
- Unique constraints : One rating per user per story

---

## 📈 Scalabilité Futures

### Phase 2 (Après présentation)
- [ ] Cache (Redis) pour stats
- [ ] Queue (Bull) pour notifications
- [ ] Search (Elasticsearch) pour histoires
- [ ] CDN pour images
- [ ] Load balancing

### Phase 3 (Long terme)
- [ ] Microservices (Games, Stats, Auth services)
- [ ] Event sourcing pour l'audit
- [ ] GraphQL pour requêtes flexibles
- [ ] Real-time features (WebSockets)

---

## 🐳 Déploiement Docker

```yaml
docker-compose.yml
├── db (MySQL 8.0)
│   └── Port 3306
├── backend (Node.js + Express)
│   ├── Port 5000
│   └── Depends on: db
└── frontend (React + Vite)
    ├── Port 3000
    └── Depends on: backend
```

**Networking** : Bridge network `nahb_network` pour communication inter-conteneurs.

---

**Dernière mise à jour** : 26/11/2025  
**Version schéma** : 1.0 (Niveau 16/20)
